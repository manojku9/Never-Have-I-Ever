{
  "name": "never-have-i-ever-backend",
  "version": "1.0.0",
  "description": "Backend for Never Have I Ever game",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
  "postinstall": "npm --prefix ../frontend install && npm --prefix ../frontend run build",
  "heroku-postbuild": "npm --prefix ../frontend install && npm --prefix ../frontend run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.5.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "socket.io": "^4.6.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}

